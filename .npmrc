# Block lifecycle scripts (preinstall, postinstall, etc.) from running automatically.
# Prevents supply-chain attacks that execute malicious code during `npm install`.
ignore-scripts=true

# Only install package versions published at least 7 days ago.
# Protects against freshly-hijacked or typosquatted packages.
min-release-age=7
